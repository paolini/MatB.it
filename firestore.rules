service cloud.firestore {
  match /databases/{database}/documents {
    match /global/public {
    	allow read;
    }
    match /notes/{note} {
      allow write;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
