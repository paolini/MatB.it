scalar ObjectId
scalar Timestamp
scalar JSON

type Note {
  _id: ObjectId!
  title: String!
  text: String
}

type Query {
  hello: String!
  note(_id: ObjectId!): Note
  notes: [Note!]!
}

type Mutation {
  newNote(title: String!, description: String): Note
}